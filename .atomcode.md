# FerrumKV — Project Instructions

> Team-shared agent instructions for the FerrumKV project. Commit this file
> to git. Personal per-user overrides go in `.atomcode.user.md` (gitignored).
> Global defaults live in `~/.atomcode/ATOMCODE.md`.

## Project Identity

- **Language**: Rust, edition 2024
- **Type**: From-scratch RESP2-compatible KV storage server (Redis wire protocol)
- **Runtime**: Tokio async, `signal-hook`, `indexmap`, zero heavy deps
- **Versions**: `Cargo.toml` = 0.3.0, README badge = v0.4.0 (known mismatch,
  track in `.issues/` if/when reconciled)

## Architecture Quick Reference

| Layer | Module | Entry points |
|---|---|---|
| Entry | `src/main.rs`, `src/cli.rs` | `main`, `build_engine`, `install_signal_handlers` |
| Network | `src/network/server.rs`, `shutdown.rs` | `run_listener`, `accept_loop`, `handle_client`, `execute_command` |
| Protocol | `src/protocol/parser.rs`, `encoder.rs` | `parse_frame`, `build_command`, `encode_*` |
| Storage | `src/storage/engine.rs`, `eviction.rs`, `expire.rs` | `KvEngine`, `pick_victim`, `sweep_expired` |
| Persistence | `src/persistence/writer.rs`, `replay.rs` | `AofWriter`, `replay` |
| Config | `src/config/file.rs` | Redis-style directive parser |
| Error | `src/error/kind.rs` | `FerrumError` (unified, 9 variants) |

Call graph exports live in `.codegraph/out/` (mermaid + DOT + SVG).

## Hard Rules (Non-Negotiable)

### Code quality gates — run before declaring any task done
```bash
cargo fmt --check                # formatting
cargo clippy --all-targets -- -D warnings   # zero warnings allowed
cargo check                      # compiles
cargo test                       # all unit + integration green
```
CI runs all four. Do not push code that fails any of them. The
`verification-before-completion` superpowers skill covers this — invoke it
before claiming completion.

### Binary safety is sacred
Keys and values are `Vec<u8>`, never assume UTF-8. Do not introduce
`String`/`str` conversions in the data path. `NUL`, `\r\n`, and non-UTF-8
sequences must round-trip through network + AOF unchanged. Existing tests
`binary_safe_*` and `non_utf8_*` guard this — keep them passing.

### Error handling
- All fallible operations return `Result<_, FerrumError>`.
- Never `.unwrap()` on runtime data; `.unwrap()` is only acceptable where a
  prior arity/guard check guarantees safety (existing pattern in
  `build_command`).
- Panics in the data path are bugs. Lock poisoning propagates via
  `From<PoisonError>` — use `?`, don't catch.
- `FerrumError::Display` text **is** the client-facing `-ERR` message — write
  it for operators, not for debugging.

### RESP2 / Redis compatibility
- FerrumKV speaks RESP2 only. Inline commands are rejected (first byte must
  be `*`).
- Redis is the reference semantics. When adding/changing a command, check
  Redis behavior first; deviations must be documented in README and have a
  test asserting the chosen behavior.
- Known incompatibilities are tracked in `.issues/` (e.g. FERRUM-001 EXISTS
  multi-key). Do not silently reintroduce a documented incompatibility.

### Persistence correctness
- Any path that removes a key (`del_many`, `sweep_expired`,
  `enforce_memory_limit`, overwrite via `set`) must call `track_remove` AND
  `aof.append_del` (if AOF enabled). This is the cross-cutting invariant —
  see `enforce_memory_limit` and `sweep_expired` for the canonical pattern.
- AOF writes are RESP2 frames, same bytes as wire protocol. Replay uses a
  separate `read_record` (not `parse_frame`) because it needs Seek + precise
  `consumed` accounting — do not unify them.

### Concurrency
- `KvEngine` interior state: `Arc<RwLock<HashMap>>` + `AtomicU64` counters
  + `Arc<Mutex<AdaptiveHybridState>>`. Write lock is held by sweep/eviction/
  mutating commands; readers (GET/INFO) take read lock or read atomics only.
- The expire sweeper runs on a **dedicated OS thread** (`thread::spawn`),
  not a tokio task — do not move it into the tokio pool (it's lock-heavy and
  would starve connection handling). The background AOF flusher is the same.
- New shared state on `KvEngine` should default to lock-free atomics where
  possible; fall back to `RwLock` only when atomic ops can't express the
  invariant.

## Issue Tracker

- Findings live in `.issues/NNN-<slug>.md` (see `.issues/README.md` for the
  full naming/format convention).
- When code review (superpowers `requesting-code-review`) surfaces a new
  defect or doc inaccuracy, record it as a new numbered issue before
  finishing.
- Mark `status: fixed` only after the fix is verified by a fast check + a
  test; reference the commit in `## Metadata`.

## Skill Defaults for This Project

These extend the global superpowers discipline in `~/.atomcode/ATOMCODE.md`:

| Task | Skill to invoke first |
|---|---|
| New command / feature | `brainstorming` → `writing-plans` → `test-driven-development` |
| Bug report / wrong output | `systematic-debugging` (reproduce before reading code) |
| Before merge / finishing branch | `requesting-code-review` → `verification-before-completion` |
| Multi-file refactor | `writing-plans` → `executing-plans` |
| Performance work | `brainstorming` (define metric) → `test-driven-development` (bench as test) |

## Testing Conventions

- Unit tests are inline `#[cfg(test)] mod tests` in each module.
- Integration tests live in `tests/` (9 suites: aof, async, timeout,
  concurrency, eviction, expire, kv_engine, max_clients, resp2_wire,
  shutdown).
- Wire-protocol behavior is tested end-to-end in `tests/resp2_wire_test.rs`
  via a real TCP listener — prefer adding cases there over mocking.
- Benches (`benches/engine_bench.rs`, `resp2_bench.rs`) use Criterion;
  run `cargo bench --no-run` in CI, full bench locally.

## Style

- 4-space indent, `cargo fmt` is authoritative.
- Public items have `///` doc comments; private helpers may use `//`.
- Match arms: exhaustiveness over `_ =>` catch-all wherever the enum is
  owned by this crate (forces compiler-driven updates on new variants).
- No `unsafe` unless justified in a `// SAFETY:` comment; currently none.

## Commit Conventions

- Conventional Commits: `feat:`, `fix:`, `test:`, `docs:`, `refactor:`,
  `bench:`, `chore:`, `ci:`.
- Reference issues as `FERRUM-NNN` in the body when relevant.
- Do not add `Co-Authored-By` trailers unless the user explicitly asks.
